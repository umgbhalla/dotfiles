#!/bin/sh

# enable builtin vi command line editor in sh
set -o vi

# aliases
[ -f $XDG_CONFIG_HOME/aliasrc ] && . $XDG_CONFIG_HOME/aliasrc

# terminal ascii
FG=$WHITE
P=$RED

msgs[0]="                          "
msgs[1]="stop ricing k?            "
msgs[2]="tell me i'm dummy thicc   "
msgs[3]="say please three times    "
msgs[4]="tell me you love me       "
msgs[5]="20 euro                   "
msgs[6]="good to know              "
msgs[7]="what is love anyways      "
msgs[8]="ur tearing me apart lisa  "
msgs[9]="stop being a karen        "
msgs[10]="stop saying i use arch btw"

msg="${msgs[$(date +%s) % ${#msgs[@]}]}"

echo -e "${FG}\
 ┌――――――――――――――――――――――――┬―――――――――┐
 ├――――――――――――――――――――――――┘ ${P}▀${FG}  ${P}▀${FG}  ${P}▀${FG} │
 │ ${P}{\__/}${NC}                           │
 │ ${P}( •.•)${NC}                           │
 │  ${P}/ >♥️${NC} u want this?               │
 │                                  │
 │ ${P}{\__/}${NC}                           │
 │ ${P}( •-•)${NC}                           │
 │ ${P} ♥️< \ ${NC}${msg} │
 └――――――――――――――――――――――――――――――――――┘
${NC}"

# scrollback
HISTFILE="$SH_HISTFILE"
HISTSIZE="1000000"

# prompt
export PS1=">> "
