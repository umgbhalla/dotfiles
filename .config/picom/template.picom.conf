# picom configuration

backend = "glx";
unredir-if-possible = true;
vsync = true;

blur-background = true;
blur-kern = "3x3box";
blur-method = "dual_kawase";
blur-strength = 3;

corner-radius = $WINDOW_BORDER_RADIUS;
