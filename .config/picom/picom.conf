# picom configuration

backend = "glx";

#shadow = true;
#shadow-radius = 5;
#shadow-offset-x = -5;
#shadow-offset-y = -5;
#shadow-opacity = 0.5;

vsync = true;

wintypes:
{
    dock =
    {
        shadow = false;
    };
};
