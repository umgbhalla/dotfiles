#!/bin/sh
# ytui wrapper

read args

if [ -z "$args" ]; then
  echo "${RED}USAGE: yt [search query]${NC}"
  exit 1
fi

$XDG_CONFIG_HOME/ytui/ytui.ts "$args" | \
  fzf | awk '{print $NF}' | \
  xargs -I {} devour mpv {}
