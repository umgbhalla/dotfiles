#!/bin/sh

cat $XDG_DATA_HOME/emojis | dmenu -l 20 -i | cut -f1 -d '#' | awk '{$1=$1};1' | tr -d '\n' | \
  xclip -selection clipboard && \
  dunstify -i network-transmit-receive-symbolic.symbolic -t 1000 \
  "Copied to clipboard."
