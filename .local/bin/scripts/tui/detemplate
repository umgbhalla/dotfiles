#!/bin/sh

if [ -f "$1" ] && [[ "$1" == *"template."* ]]; then
  filename=$(basename "$1" | sed -E 's/^template\.//')
  output="$(dirname "$1")/$filename"
  envsubst < $1 > $output
else
  echo "template does not exist, or file name is invalid."
fi
