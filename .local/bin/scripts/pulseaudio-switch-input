#!/bin/sh
input=$(pacmd list-sources | awk '/name:.*input/{print substr($2, 2, length($2)-2)}' | dmenu -l 10 -p "Input Sink:")

if [ -z $input ]; then
  echo "No sink specified."
else
  pacmd set-default-source $input
  # move inputs to $input?
  echo "Default source set to $input."
fi
