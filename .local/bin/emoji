#!/bin/sh

emoji=$(cat $XDG_DATA_HOME/emojis | $SHELLMENU | \
  awk -F '#' '{printf "%s",$1}')

if [ -n "$emoji" ]; then
  echo $emoji | tr -d '\n' | xclip -selection clipboard
  dunstify -i network-transmit-receive-symbolic.symbolic -t 1000 "Copied to clipboard."
fi
