#!/bin/sh
# a program launching script

case $OS in
  $OS_FREEBSD)
    $(\
      echo $PATH \
      | tr ':' '\n' \
      | xargs -I {} find {} -type f \
      | xargs basename \
      | $SHELLMENU \
    )
    ;;
  *)
    $(\
      ls /bin \
      | $SHELLMENU \
    )
    ;;
esac
